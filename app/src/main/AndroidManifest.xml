<?xml version="1.0" encoding="UTF-8"?>

    <manifest package="com.example.user.myapplication" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application android:theme="@style/AppTheme.NoActionBar" android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:allowBackup="true">

        <uses-library android:name="org.apache.http.legacy"
            android:required="false"/>
    <activity android:name=".MainActivity">


    <intent-filter>

    <action android:name="android.intent.action.MAIN"/>

    <category android:name="android.intent.category.LAUNCHER"/>

</intent-filter>

</activity>

    <activity android:name=".GraphActivity"/>

    <activity android:name=".DayAxisValueFormatter"/>

    <activity android:name=".MyAxisValueFormatter"/>

    <activity android:name=".XYMarkerView"/>

    <activity android:name=".LoginActivity"/>

   <activity android:name=".RegisterActivity"/>

    <activity android:name=".RegisterF"/>

    <activity android:name=".activity_mypage"/>

    <activity android:name=".chg_mypage"/>

    <activity android:name=".score"/>

    <activity android:name=".listConActivity"/>

    <activity android:name=".Tetris"/>

        <activity android:name=".alert"/>
        <activity android:name=".alert_listContract"/>
        <activity android:name=".contract_chg"/>

    <activity android:name=".matchActivity"/>

    <activity android:name=".ContractActivity"/>

    <activity android:name=".ContractCheckActivity"/>

    <activity android:name=".ScoreActivity"/>

    <activity android:name=".puzzleActivity"/>

    <!--<activity android:name=".cutPhoto" />-->


    <activity android:name=".PhotoUploadActivity"/>

    <activity android:name=".RandomNumberActivity"/>

    <activity android:name=".ContrastPlusActivity"/>






        <service

            android:name=".FireBaseMessagingService">

            <intent-filter>

                <action android:name="com.google.firebase.MESSAGING_EVENT"/>

            </intent-filter>

        </service>



        <service

            android:name=".FirebaseInstanceIDService">

            <intent-filter>

                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>

            </intent-filter>

        </service>


</application>

</manifest>